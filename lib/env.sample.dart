// for example "localhost:3000"
const backendUrl = '';
const isHttps = false;

// Generate a secret key using the following command:
// openssl rand -base64 32
// same as secret in backend environment
const secret = "";
